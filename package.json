{
  "name": "openwhyd-electron",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "postinstall": "# brew install wine # in order to build for win32 from macos/darwin",
    "build": "electron-packager . Openwhyd --icon \"icon\" --out build --overwrite --all",
    "build-darwin": "electron-packager . Openwhyd --icon \"icon\" --out build --overwrite --platform=darwin",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^1.4.13",
    "electron-packager": "^8.4.0"
  }
}
